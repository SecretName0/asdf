// 포인터 연산
/*
int data[5] = { 1,2,3,4,5 };

int* ptr = data;

printf("ptr이 가라키는 값 : %d\n", *ptr);

ptr = ptr + 2;

printf("ptr을 증가시킨 후 가리키는 값 : %d\n", *ptr);

// 1
// 홀수
ptr = ptr - 1;

// 10
// 짝수
printf("ptr을 감소시킨 후 가리키는 값 : %d\n", *ptr);

return 0;
}
*/